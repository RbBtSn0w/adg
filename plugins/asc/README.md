# asc

App Store Connect workflows and TestFlight upload.

This is a reference ADG plugin. The universal manifest lives at
[.adg-plugin/plugin.json](.adg-plugin/plugin.json); runtime manifests under
`.claude-plugin/` and `.codex-plugin/` are generated by `adg plugin adapt`.
