# The `.agents/` Directory Specification

**Status:** Draft · **Schema:** [`adg-plugin.schema.json`](../schemas/adg-plugin.schema.json) (`adg.plugin/v1`) · **Steward:** ADG

`.agents/` is the canonical, vendor-neutral home for a plugin's **source**
manifest. ADG does not own a proprietary `.adg-*` format; its job is to keep the
`.agents/` directory consistent and to project it onto each runtime.

The key words MUST, SHOULD, and MAY follow RFC 2119.

---

## 1. Layout

A plugin is authored once under `.agents/` and projected to each runtime:

```
asc/                              # a plugin (authoring source)
├── .agents/
│   └── .plugin.json              # canonical source manifest (source of truth)
├── skills/<name>/SKILL.md        # declared payload
├── commands/  agents/  .mcp.json # declared payload
└── README.md  LICENSE  CHANGELOG # canonical metadata
# .claude-plugin/ , .codex-plugin/ are NOT authored here — they are runtime
# projections, generated at install time (or committed only when publishing to a
# runtime's native registry).
```

- The source manifest lives at **`.agents/.plugin.json`**. The dot-prefixed
  filename marks it as a production/source artifact; the `.agents/` directory is
  the neutral namespace.
- A repo authoring a catalog of plugins MAY declare it at
  **`.agents/.marketplace.json`** (schema:
  [`marketplace.schema.json`](../schemas/marketplace.schema.json)).
- Runtime **projections** (`.claude-plugin/`, `.codex-plugin/`) are NOT authoring
  artifacts: `adg plugins add` generates them into the consumer tree at install
  time. They are committed in a source repo only to publish to a runtime's native
  registry (via `adg plugins adapt`), and MUST NOT be hand-edited.

### Legacy fallback

The previous location `.adg-plugin/plugin.json` is **deprecated** but still read:
`readManifest` and discovery (`scanPlugins`) prefer `.agents/.plugin.json` and
fall back to `.adg-plugin/plugin.json` (`src/manifest.ts:findManifestFile`). New
plugins MUST use `.agents/.plugin.json`.

### Consumption is unchanged

Installed plugins still land under `.agents/plugins/` (`src/paths.ts`), and the
Codex/Claude-facing export keeps its plain name —
`.agents/plugins/marketplace.json` and `.claude-plugin/marketplace.json`. These
are **runtime exports**, distinct from a source `.agents/.marketplace.json`, and
MUST keep their plain filenames so the runtimes discover them.

---

## 2. Manifest — `.agents/.plugin.json`

The manifest stores **path pointers**, never inlined payload; the same payload
directory (e.g. `./skills/`) is referenced by the source manifest and reused by
every projection. See the schema for the full field set; the load-bearing fields:

```jsonc
{
  "schemaVersion": "adg.plugin/v1",
  "name": "asc",                         // kebab-case (REQUIRED)
  "version": "0.1.0",                     // semver (REQUIRED)
  "description": "App Store Connect …",   // non-empty (REQUIRED)
  "skills": "./skills/",                  // string (dir) | array (explicit)
  "commands": "./commands/",
  "agents": "./agents/",
  "mcpServers": "./.mcp.json",
  "dependencies": [{ "name": "github-cr", "version": "^0.2.0" }],
  "strict": true
}
```

Projection output paths (`.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`)
are ADG-internal conventions, not declared in the manifest.

---

## 3. Packaging — manifest-driven allowlist

When a plugin is installed (copied/cloned), only its **declared payload** ships;
the authoring repo's dev files (`src/`, `test/`, `docs/`, CI config, …) are left
behind. This is default-deny — safer (no accidental secret/dev-file leakage) and
consistent with the content hash.

The packaged file set (`src/package.ts:packagedRoots` / `packageFilter`) is:

- the `.agents/` manifest home (and legacy `.adg-plugin/` during deprecation);
- the top-level path segment of every declared component pointer
  (`skills`/`commands`/`agents`/`mcpServers`/`hooks`/`apps`);
- canonical metadata at the root: `README*`, `LICENSE*`, `CHANGELOG*`, `NOTICE*`;
- the runtime projections `.claude-plugin/`, `.codex-plugin/` when present (copy
  only) — at install they are regenerated into the destination regardless.

The **same** allowlist drives both the copy (`copyPluginDir`) and the content
hash (`folderHash`), so an in-place plugin (copy skipped) and a copied install
hash identically. Projections are excluded from the hash so re-adapting does not
churn it.

A plugin needing finer exclusion *within* a component directory SHOULD use git's
native `.gitattributes export-ignore` rather than a bespoke ignore format.

---

## 4. Consistency (the `validate` gate)

`adg plugins validate` is the gate for any source manifest — whether authored by
a human or generated by an agent. Beyond JSON-schema validity it SHOULD enforce:

- **Pointer integrity** — every path pointer (`skills`/`commands`/`agents`/
  `mcpServers`/`hooks`/`apps`) resolves; every referenced `SKILL.md` has a non-empty
  `name` and `description`.
- **Projection sync** — *if* a source repo commits projections (to publish to a
  native registry), they must match a fresh projection of the current
  `.agents/.plugin.json` (no hand-edited drift). Absent projections are fine.
- **Dependency resolvability** — each `dependencies[]` entry resolves to a known
  plugin in the reachable catalog(s).

---

## 5. Migration from `.adg-plugin/`

1. Move `.adg-plugin/plugin.json` → `.agents/.plugin.json` (content unchanged).
2. Run `adg plugins update` so lock `folderHash` values reflect the new layout.
3. `adg plugins validate` to confirm. (Re-run `adg plugins adapt` only if you
   commit projections for a native registry.)

ADG reads the legacy `.adg-plugin/plugin.json` for one release to ease the move.



---

## 6. Supported Agents — Plugin Paths

| Agent | Scope | Configuration / Plugin Paths | Details |
| :--- | :--- | :--- | :--- |
| **Claude** | Repo (Project) | `$REPO_ROOT/.claude/settings.json` | Team plugins shared via version control. |
| | Personal (Global) | `~/.claude/settings.json` | Personal plugins available across all projects (default). |
| **Codex** | Repo (Project) | Marketplace: `$REPO_ROOT/.agents/plugins/marketplace.json`<br>Plugins: `$REPO_ROOT/plugins/` | Add a marketplace file and store your workspace-specific plugins. |
| | Personal (Global) | Marketplace: `~/.agents/plugins/marketplace.json`<br>Plugins: `~/.codex/plugins/` | Add a marketplace file and store your global plugins. |
| **Antigravity** | Repo (Project) | `$REPO_ROOT/.agents/plugins/` | Place your plugin folder here; only available within this workspace. |
| | Personal (Global) | `~/.gemini/config/plugins/` | Place your plugin folder here; active across all workspaces. |

---

## 7. Agent Adapter Template

Below is the **canonical template** every agent adapter MUST document. When adding
a new agent, copy this template and fill in the blanks. All existing agent sections
below follow this structure, making cross-agent comparison and adapter development
straightforward.

```
### <Agent Name>

#### 7.x.1 Official References
- Plugin docs: <URL>
- Manifest schema: <URL>

#### 7.x.2 Projection Output
- Output directory: <path relative to plugin root>
- Manifest file: <filename>
- Adapter source: `src/adapters/<file>.ts`

#### 7.x.3 Manifest Schema
- Required fields: ...
- Optional fields: ...
- Example (minimal):

#### 7.x.4 Supported Components
| Component | Supported | Manifest Key | Discovery Mechanism |
| :--- | :---: | :--- | :--- |

#### 7.x.5 Skills
- Declared form: ...
- Strict mode: ...
- Array encoding: ...
- Auto-discovery: ...

#### 7.x.6 MCP Servers
- Config file name: ...
- Config key: ...
- Special handling: ...

#### 7.x.7 Hooks
- Canonical file: ...
- Native override: ...
- Supported events: ...
- Event mapping: ...
- Hook types: ...
- Environment bridging: ...

#### 7.x.8 Environment Variables
- Plugin root: ...
- Substitution behavior: ...
```

Each agent's full specification lives in its own file under `docs/agents/`:

| Agent | Spec File |
| :--- | :--- |
| Claude (Anthropic) | [`agents/claude.md`](./agents/claude.md) |
| Codex (OpenAI) | [`agents/codex.md`](./agents/codex.md) |
| Antigravity (Gemini) | [`agents/antigravity.md`](./agents/antigravity.md) |

When adding a new agent, create `docs/agents/<agent>.md` using the template above,
then add a row to this table.

---

## 8. Cross-Agent Component Matrix

Summary of component support across all agents. This is the authoritative view for
deciding what an ADG plugin can expose to each runtime.

| Component | Claude | Codex | Antigravity | ADG Source Key |
| :--- | :---: | :---: | :---: | :--- |
| **Skills** | ✅ | ✅ | ✅ | `skills` |
| **Agents** | ✅ | ❌ | ✅ | `agents` |
| **Commands** | ✅ | ❌ | ✅ | `commands` |
| **Hooks** | ✅ | ✅ | ✅ | `hooks` |
| **MCP** | ✅ | ✅ | ✅ | `mcpServers` |
| **Apps** | ✅ | ❌ | ❌ | `apps` |
| **LSP** | ✅ | ❌ | ❌ | N/A (Claude-only) |
| **Monitors** | ✅ | ❌ | ❌ | N/A (Claude-only) |
| **Themes** | ✅ | ❌ | ❌ | N/A (Claude-only) |

Source: [`ADAPTER_COMPONENTS`](../src/adapters/index.ts) in `src/adapters/index.ts`.

---

## 9. Cross-Agent Skills Encoding

Skills are the most universal component. Their manifest encoding differs per agent:

| Aspect | Claude | Codex | Antigravity |
| :--- | :--- | :--- | :--- |
| **Root string** | `"./skills/"` — passed through | `"./skills/"` — passed through | N/A — always auto-scans `skills/` |
| **Array form** | `["./skills/foo", "./skills/bar"]` (path form) | `["foo", "bar"]` (bare-id form) | N/A — no manifest array |
| **Strict default** | `true` — directory discovery | `true` — directory discovery | Always directory discovery |
| **Non-strict** | Explicit id list → `strict: false` | Explicit id list | N/A |
| **SKILL.md** | Required per skill sub-dir | Required per skill sub-dir | Required per skill sub-dir |
| **Frontmatter** | `name`, `description` (required) | `name`, `description` (required) | `name`, `description` (required) |

**Unified rule**: All agents discover skills as sub-directories of `skills/` containing
a `SKILL.md` with YAML frontmatter. The SKILL.md format is identical across all agents.

---

## 10. Cross-Agent Hooks Comparison

Hooks require the most complex adapter logic. See also
[`docs/hooks-strategy.md`](./hooks-strategy.md) for the design rationale.

| Aspect | Claude | Codex | Antigravity |
| :--- | :--- | :--- | :--- |
| **Canonical format** | Claude's format is canonical | Same as Claude | Native schema (different) |
| **Authoring file** | `hooks/hooks.json` | `hooks/hooks.json` or `hooks/hooks-codex.json` | `hooks/hooks-antigravity.json` or translated from canonical |
| **Discovery** | Auto-loaded from `hooks/hooks.json` | Explicit manifest reference (no auto-load) | Root `hooks.json` at plugin root |
| **Supported events** | 30 events | 10 events | 4 events |
| **Hook types** | `command`, `http`, `mcp_tool`, `prompt`, `agent` | `command` | `command` |
| **Matcher** | Regex string | Regex string | Simple tool-name alternation |
| **Translation by ADG** | None (canonical) | Minimal (file resolution) | Full (schema + event + tool-name + I/O bridge) |

### Hooks authoring strategy

| Scenario | What to do |
| :--- | :--- |
| Same behavior across all agents | Author one `hooks/hooks.json` in Claude's format. ADG routes and translates. |
| Different behavior per agent | Ship native override files: `hooks/hooks-codex.json`, `hooks/hooks-antigravity.json`. ADG routes each to the right target. |
| Antigravity-only behavior | Author `hooks/hooks-antigravity.json` in Antigravity's native schema. Bypasses translation. |

---

## 11. Cross-Agent MCP Comparison

| Aspect | Claude | Codex | Antigravity |
| :--- | :--- | :--- | :--- |
| **Config file** | `.mcp.json` or inline in manifest | `.mcp.json` reference | `mcp_config.json` at plugin root |
| **Manifest key** | `mcpServers` | `mcpServers` | N/A (discovered by file presence) |
| **Format** | `{ "mcpServers": { "<name>": { "command": ... } } }` | Same as Claude | Same as Claude |
| **Projection** | Passed through | Passed through | Copied from source `.mcp.json` to `mcp_config.json` |

---

## 12. Adding a New Agent

When support for a new agent runtime is needed:

1. **Create adapter source** at `src/adapters/<agent>.ts` implementing `AdapterFn`.
2. **Register** in [`src/adapters/index.ts`](../src/adapters/index.ts):
   - Add to `ADAPTERS` map with agent name and any aliases.
   - Add to `ADAPTER_TARGETS`.
   - Add to `ADAPTER_COMPONENTS` with the supported component types.
3. **Document** by copying the template in §7 and filling in all sections.
4. **Update cross-agent matrices** (§8–§11) to include the new agent column.
5. **Add hook event support** in [`src/hooks.ts`](../src/hooks.ts) `SUPPORTED_EVENTS`.
6. **Test** via `adg plugins adapt --target <agent>` and `adg plugins validate`.

